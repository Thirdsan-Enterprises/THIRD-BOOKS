name: 🚀 Deploy Landing Page to Production

on:
  push:
    branches:
      - main
      - 'claude/thirdbooks-accounting-system-*'
  pull_request:
    branches:
      - main
      - 'claude/thirdbooks-accounting-system-*'
    types: [ closed ]
  workflow_dispatch:
    inputs:
      reason:
        description: 'Deployment reason'
        required: false
        default: 'Manual deployment trigger'

concurrency:
  group: deploy-landing-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    # Run on push to main OR when PR is merged to main (not just closed)
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)

    steps:
      - name: 🚀 Starting Landing Page Deployment
        run: |
          echo "============================================"
          echo "  🌐 DEPLOYING THIRDBOOKS LANDING PAGE"
          echo "============================================"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Triggered by: ${{ github.actor }}"
          echo "Reason: ${{ github.event.inputs.reason || 'Automatic deployment' }}"
          echo ""

      - name: Checkout code
        uses: actions/checkout@v4

      - name: 📂 List files to deploy
        run: |
          echo "Files in landing-page directory:"
          ls -laR landing-page/
          echo ""
          echo "Total size:"
          du -sh landing-page/

      - name: 📤 Deploy to thirdbooks.digital via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./landing-page/
          server-dir: /domains/thirdbooks.digital/public_html/
          dangerous-clean-slate: false
          log-level: verbose
          exclude: |
            **/.git*
            **/.DS_Store
            **/README.md

      - name: 🔍 Verify deployment
        run: |
          echo "Deployment completed. Checking accessibility..."
          echo ""
          echo "Expected URL: https://thirdbooks.digital"
          echo "Expected files uploaded:"
          echo "  - index.html"
          echo "  - css/style.css"
          echo "  - js/main.js"
          echo "  - images/favicon.svg"
          echo ""
          echo "If site doesn't load, check:"
          echo "  1. FTP path is correct: /domains/thirdbooks.digital/public_html/"
          echo "  2. Files have proper permissions (644 for files, 755 for folders)"
          echo "  3. index.html is in the root of public_html/"

      - name: ✅ Deployment Summary
        if: success()
        run: |
          echo "============================================"
          echo "  ✅ LANDING PAGE DEPLOYED SUCCESSFULLY!"
          echo "============================================"
          echo ""
          echo "🌐 URL: https://thirdbooks.digital"
          echo "📦 Branch: ${{ github.ref_name }}"
          echo "📝 Commit: ${{ github.sha }}"
          echo "⏰ Time: $(date -u)"
          echo ""
          echo "Next steps:"
          echo "  1. Visit https://thirdbooks.digital"
          echo "  2. Verify landing page loads correctly"
          echo "  3. Test all sections and links"
          echo ""
